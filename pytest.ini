[pytest]
markers =
    slow: compiles a real installed chart (tens of seconds each). `make test`
        deselects these; `make test-all` runs them. Everything under
        tests/local/ is marked automatically - see tests/conftest.py. CI runs
        the whole suite, where these skip for want of the game installs.
